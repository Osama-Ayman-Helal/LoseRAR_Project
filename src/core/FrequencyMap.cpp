#include "FrequencyMap.h"
#include <fstream>
#include <iostream>

std::unordered_map<unsigned char, int> FrequencyMap::buildFrequencyMap(const std::string& filePath) {
    std::unordered_map<unsigned char, int> map;
    // TODO: Student 1 - Write your file reading loop here
    return map;
}
