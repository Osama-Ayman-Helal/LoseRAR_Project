#include "Decoder.h"

bool Decoder::decodeBit(Node* root, Node*& currentPosition, char bit, unsigned char& resultChar) {
    // TODO: Student 4 - Implement tree traversal state machine
    return false; 
}
